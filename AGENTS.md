# 项目协作规则

- 这是可独立审查的飞书 MCP + Skill 发布仓库；私有管理仓库只能作为只读参考源。
- 不复制 `.env`、令牌、账号信息、测试文档内容或其他隐私数据。示例配置只允许占位符。
- 引入或改造上游内容前，先核对许可证、来源和署名要求，并保留第三方通知。
- MCP 与 Skill 分目录维护，通过稳定、可测试的接口契约衔接，避免彼此复制实现细节。
- 涉及真实飞书写入时，先使用专用测试文档并取得用户确认；普通测试优先 mock、dry-run 或临时数据。
- 任何公开发布、版本标签、可见性变更或远端写入，都必须在用户明确授权后执行。
